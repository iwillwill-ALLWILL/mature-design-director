{
  "schema_version": 1,
  "primary": "replace with the natural artifact entrypoint inside the ZIP",
  "files": [
    {
      "path": "replace with an exact payload path",
      "sha256": "replace with the lowercase SHA-256 of that payload"
    }
  ]
}
